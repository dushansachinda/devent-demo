configurable int servicePort = 8080;
configurable string serviceHost = "localhost";
configurable string databaseUrl = "jdbc:h2:mem:hoteldb";
configurable string apiVersion = "v1";
configurable string apikey = ?;